import { SubjectCategoryPipe } from './subject-category.pipe';

describe('SubjectCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new SubjectCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
